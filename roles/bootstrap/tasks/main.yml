---
- name: Import Tasks
  ansible.builtin.include_tasks: "{{ task }}"
  loop:
    - locale.yml
    - packages.yml
    - user.yml
  loop_control:
    loop_var: task
  when:
    - ansible_os_family == 'Debian' or ansible_os_family == 'Ubuntu'
